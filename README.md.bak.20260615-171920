# CodeAgentLens

[English](docs/README_EN.md) | [简体中文](README.md)

CodeAgentLens 是面向代码代理的本地模型端点网关与观测工作台。它为 Claude Code、Codex 以及其他兼容 OpenAI/Anthropic 协议的客户端提供统一的本地网关，同时把 trace、prompt、请求 artifact、token 用量、endpoint 决策和日志保留在本机可检查的证据链中。

本项目是 ccNexus 的独立维护衍生项目。

上游项目：https://github.com/lich0821/ccNexus

## CodeAgentLens 提供什么

- 面向代码代理模型流量的 loopback Gateway/API：`http://127.0.0.1:3010`。
- 独立的 Debug Portal：`http://127.0.0.1:3011/debug/obs`，用于观测和本地 artifact 检查。
- 导出到本地观测工具栈的 OpenTelemetry traces 和 metrics。
- 本地请求 dump，包含 prompt 提取、headers、请求/响应 body、转换后的 body、stream events 和 usage 文件。
- 面向 Claude、OpenAI Chat、OpenAI Responses 风格客户端的 endpoint 路由、重试、凭证轮换和 token 用量统计。
- 可纳入版本控制的本地验证命令，覆盖 native 和 Docker 运行模式。

## 架构图

![CodeAgentLens 架构图](docs/images/diagrams/diagram-code-agent-lens-architecture-zh.svg)

*图 1：代码代理流量如何穿过 Gateway，并被记录为本地 traces、dump artifacts 与 Debug Portal 入口。*

## 部署图

![CodeAgentLens 部署图](docs/images/diagrams/diagram-code-agent-lens-deployment-zh.svg)

*图 2：本机部署中 Gateway、Debug Portal、dump root、SQLite 和本地观测栈之间的连接关系。*

默认本地入口：

| 组件 | URL | 用途 |
| --- | --- | --- |
| Gateway/API | `http://127.0.0.1:3010` | 本地代码代理模型 endpoint。 |
| Debug Portal | `http://127.0.0.1:3011/debug/obs` | 查看 traces、prompts、artifacts 和工具入口的主界面。 |
| Jaeger | `http://127.0.0.1:16686` | Trace 搜索与 span timing。 |
| Grafana | `http://127.0.0.1:13000` | Dashboard 与 Tempo/Prometheus 查询。 |
| Prometheus | `http://127.0.0.1:9090/graph` | Metrics 查询界面。 |
| Tempo | `http://127.0.0.1:3200/status` | Trace storage 状态。 |
| OTel Collector | `http://127.0.0.1:8888/metrics` | Collector 诊断指标。 |

## 观测功能

CodeAgentLens 将流量处理与观测访问分离。Gateway/API 监听 `3010`，代码代理客户端只需要配置这个 URL。Debug Portal 监听 `3011`，用于检查请求流经 gateway 后发生了什么。

### 关键本地配置

| 配置项 | 作用 |
| --- | --- |
| `CODE_AGENT_LENS_OBS_LOCAL_DEBUG=true` | 启用本地 debug policy。 |
| `CODE_AGENT_LENS_OBS_DUMP_ENABLED=true` | 启用本地请求 artifact dump。 |
| `CODE_AGENT_LENS_OBS_DUMP_DIR=<path>` | 指定 Debug Portal 读取的 dump root；Docker profile 使用 `/data/observability`。 |
| `CODE_AGENT_LENS_OBS_VIEWER_ENABLED=true` | 启用 Debug Portal。 |
| `CODE_AGENT_LENS_OBS_VIEWER_PUBLIC_URL=http://127.0.0.1:3011/debug/obs` | 写入 trace attributes 与 artifact links 的本地 viewer URL。 |
| `CODE_AGENT_LENS_OBS_CAPTURE_HEADERS=all` | 采集请求与上游 headers；secret headers 默认 redacted。 |
| `CODE_AGENT_LENS_OBS_CAPTURE_BODIES=all` | 采集 ingress、transform 和 upstream body artifacts。 |
| `CODE_AGENT_LENS_OBS_CAPTURE_STREAM_EVENTS=all` | 采集 raw 与 transformed streaming event JSONL。 |
| `CODE_AGENT_LENS_OBS_PROMPT_EXTRACT=true` | 从支持的 API body 中提取 prompt role artifacts。 |

### 集成模块

| 模块 | 功能 | 查看位置 |
| --- | --- | --- |
| Gateway listener | 接收来自代码代理的 Claude、OpenAI Chat、OpenAI Responses 风格请求。 | `http://127.0.0.1:3010/health` 和 gateway 日志。 |
| Endpoint resolver | 选择当前启用的活动 endpoint，支持手动切换，并在轮换时保护 in-flight endpoint requests。 | Web UI 和请求日志。 |
| Protocol transformers | 在受支持的客户端格式和上游格式之间转换，包括 Claude、OpenAI Chat、OpenAI Responses 路径。 | `transform.request.*` 请求 artifact 和转换后的响应 artifact。 |
| Proxy and retry engine | 使用连接池发送上游请求，重试瞬时失败，跟踪 active requests，并记录 endpoint failures。 | Debug Portal 请求详情、日志和 usage artifacts。 |
| Token and usage extractor | 从非流式和流式响应中规范化 input/output token 用量。 | 每个请求 artifact 目录中的 `usage.json`。 |
| Request recorder | 创建 request ID、trace ID、span ID、obs reference、viewer URL 和 OTel attributes。 | `/debug/obs/trace/{trace_id}` 和 Jaeger span attributes。 |
| Local dump writer | 在 `runs/{run_id}/traces/{trace_id}/{request_id}` 下写入 manifest-bound 请求 artifacts。 | `/debug/obs/request/{request_id}`。 |
| Prompt extractor | 从受支持 API body 中提取 system、developer、user 和其他 prompt roles。 | `/debug/obs/prompts` 与 `/debug/obs/request/{request_id}/prompts`。 |
| Stream event capture | 启用 stream capture 后记录 raw 和 transformed SSE events。 | Stream artifacts 和 `/debug/obs/request/{request_id}`。 |
| Debug Portal | 提供 recent requests、prompt sessions、errors、trace links、raw artifacts 和集成工具的本地 UI。 | `http://127.0.0.1:3011/debug/obs`。 |
| Jaeger integration | 展示分布式 trace timing，并通过 `code-agent-lens_obs_ref` 链回 CodeAgentLens artifacts。 | `/debug/obs/tool/jaeger` 或原生 Jaeger。 |
| Grafana integration | 打开本地 dashboards 与 Tempo/Prometheus views，同时保留 Portal navigation。 | `/debug/obs/tool/grafana` 或原生 Grafana。 |
| Prometheus metrics | 暴露 requests、errors、retries、endpoint rotations、stream events、credential refreshes 和 token totals 的 counters/histograms。 | Prometheus 或 Grafana。 |
| Validation CLI | 为 stack profiles、local debug policy、synthetic traces、auth simulations 和 port checks 生成 evidence。 | `go run ./cmd/code-agent-lens obs ...`。 |

### 采集的 Artifacts

对每个可观测请求，CodeAgentLens 可以写入一个 manifest-bound 目录，包含：

- `prompt.index.json`：请求 metadata、prompt role manifest、file manifest、trace ID、span ID、request ID 和 run ID。
- `ingress.request.headers.json` 与 `ingress.request.body.raw`：客户端请求数据；除非显式启用本地密钥采集，否则 secret headers 会被 redacted。
- `prompt.system.txt`、`prompt.developer.txt`、`prompt.user.txt` 和编号变体：按 role 提取的 prompt 文本。
- `transform.request.input.raw` 与 `transform.request.output.raw`：transformer 输入和输出快照。
- `upstream.request.*` 与 `upstream.response.*`：上游 URL、headers、body 和转换后的响应数据。
- `stream.raw.events.jsonl` 与 `stream.transformed.events.jsonl`：启用后记录的 streaming event 证据。
- `usage.json`：规范化 input/output token 数量和 usage 数据来源。

Debug Portal 通过 manifest-bound routes 提供 artifact 访问，不暴露任意文件系统路径。

## 快速开始

运行静态 source validation：

```powershell
go run ./cmd/code-agent-lens obs validate --deployment-profile local_debug --profile deploy/observability/stack.local.yaml --evidence-dir .tmp/release-gate/observability/native
```

当 `3010` 和 `3011` 端口空闲时，启动 native local debug runtime：

```powershell
go run ./cmd/code-agent-lens obs up --mode native --deployment-profile local_debug --profile deploy/observability/stack.local.yaml --evidence-dir .tmp/release-gate/observability/native
```

runtime 启动后运行 full-chain synthetic trace validation：

```powershell
go run ./cmd/code-agent-lens obs validate --mode native --deployment-profile local_debug --profile deploy/observability/stack.local.yaml --synthetic --trace --evidence-dir .tmp/release-gate/observability/native
```

停止 native runtime：

```powershell
go run ./cmd/code-agent-lens obs stop --mode native --profile deploy/observability/stack.local.yaml --evidence-dir .tmp/release-gate/observability/native-stop
```

检查官方端口是否空闲：

```powershell
go run ./cmd/code-agent-lens obs ports --expect-free 3010,3011 --evidence-dir .tmp/release-gate/observability/native-stop
```

Docker mode 可在官方端口可用后使用内置 compose profile：

```powershell
docker compose -f deploy/observability/docker-compose.full.yaml up -d --build
```

## 常用用户场景 Q/A

### Q: 我想知道代码代理实际发送了什么 prompt，在哪里看？

打开 `http://127.0.0.1:3011/debug/obs/prompts`。使用 search、role 和 run filters 找到请求，再打开该请求的 prompt details，即可查看 system、developer、user 和其他 prompt roles。

### Q: 我有 trace ID，如何打开相关 request artifacts？

打开 `http://127.0.0.1:3011/debug/obs/trace/{trace_id}`。Trace 页面会列出该 trace 下发现的 request IDs，并链接到 request details、prompt details、raw bodies 和 artifact views。

### Q: 我从 Jaeger 拿到了 `obs_ref`，如何跳回 CodeAgentLens？

打开 `http://127.0.0.1:3011/debug/obs/ref/{trace_id}/{request_id}`。Jaeger 也可以使用 `docs/observability/jaeger-ui-config.json` 让 `code-agent-lens_obs_ref` 成为可点击链接。

### Q: 我想确认 agent 使用的是 Claude Messages、OpenAI Chat 还是 OpenAI Responses 格式，怎么看？

打开 request detail 页面，查看 ingress body 和 transformer artifacts。Gateway 会根据 `/v1/messages`、`/v1/chat/completions`、`/v1/responses` 等 path 检测 client format。

### Q: 我想检查 endpoint 决策，或为什么请求去了某个 provider，怎么看？

从 `/debug/obs` 打开对应请求，然后检查 upstream request artifacts 和日志。Endpoint state 也会显示在 Web UI 中，并持久化到本地 SQLite 配置和 stats store。

### Q: 我想比较原始上游数据和转换后的输出，怎么看？

打开 `/debug/obs/request/{request_id}`。使用 artifact links 查看 ingress request body、transform input、transform output、upstream request body、upstream response body 和 upstream response transformed body。

### Q: 我需要单个 agent 请求的 token 用量，怎么看？

打开 `/debug/obs/request/{request_id}` 并查看 `usage.json`。它记录 normalized input/output tokens，以及 usage 来自 upstream 数据还是 fallback extraction。

### Q: 我需要调试 streaming response，怎么看？

打开 request detail 页面并检查 stream artifacts。启用 stream capture 后，CodeAgentLens 会写入 raw 和 transformed stream event JSONL 文件，方便比较 upstream SSE 形态和 client-facing SSE 形态。

### Q: 我想知道代码代理请求为什么慢，怎么看？

打开 `/debug/obs/tool/jaeger` 或原生 Jaeger `http://127.0.0.1:16686`。搜索 CodeAgentLens service，跟踪 gateway request handling、ingress capture、upstream request、usage extraction 和 stream processing 等 spans。

### Q: 我想看 dashboards 或 metrics，而不是 raw traces，在哪里看？

打开 `/debug/obs/tool/grafana` 或原生 Grafana `http://127.0.0.1:13000`。Prometheus metrics 包含 request totals、error totals、request duration、upstream duration、retries、endpoint rotations、token counters、stream events 和 credential refresh/failure counters。

### Q: 我想确认 Portal 与 Gateway/API 是分离的，怎么看？

Gateway traffic 使用 `127.0.0.1:3010`。Debug Portal traffic 使用 `127.0.0.1:3011/debug/obs`。Gateway 会故意对 `/debug/obs` 返回 not found，避免 agent tokens 和模型调用流量打开 Portal listener。

### Q: 我只想本地调试，CodeAgentLens 会把 prompt 发到远端服务吗？

Debug Portal 从配置的 dump root 读取本地 dump 文件。完整 prompt/body artifacts 是本地 debug artifacts。OpenTelemetry traces 和 metrics 会导出到内置本地工具栈中配置的 local collector。

### Q: 如果 `3010` 或 `3011` 已经被占用怎么办？

停止占用官方端口的进程，或运行不作为正式 release evidence 的独立诊断环境。官方 local debug profile 使用 `3010` 作为 Gateway/API，使用 `3011` 作为 Debug Portal。

### Q: 如何证明观测路径端到端可用？

先运行 `obs up`，再运行 `obs validate --synthetic --trace`。验证会将 fixture-driven OpenAI Responses 请求通过本地 gateway，检查响应、本地 dump artifacts、prompt roles 和 usage，并把 evidence 写入指定目录。

### Q: 如何配置 Claude Code 或 Codex 让流量进入 CodeAgentLens？

根据客户端类型，把 agent base URL 指向 `http://127.0.0.1:3010` 或 `http://127.0.0.1:3010/v1`。先使用 `obs configure-agents --backup` dry run 预览 agent 配置，不直接修改用户配置文件。

## 安全模型

默认 `local_debug` profile 用于 loopback-only 开发。它可以启用完整本地采集，让开发者在同一台机器上检查 prompt 和模型 payload。非 loopback profiles 必须 fail closed，除非 auth、RBAC、capture policy 和 tool exposure 规则通过验证。

重要边界：

- `local_debug` 是唯一允许关闭 Portal auth 的 profile，并且仅限 loopback bind。
- `lan_team` 和 `public_server` 使用 metadata-first capture defaults，并要求 auth/RBAC simulation 通过。
- `Authorization`、`X-API-Key` 等 secret headers 会被 redacted，除非显式启用本地 debug capture。
- Raw artifact routes 是 manifest-bound，并拒绝 path traversal。
- Agent Gateway traffic 和 Portal traffic 使用独立 listener。

## 维护者

维护者：milome

安全报告：GitHub private vulnerability reporting: https://github.com/milome/code-agent-lens/security/advisories/new
